// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: analytics.sql

package generated

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const getEventBookingSummary = `-- name: GetEventBookingSummary :one
SELECT 
    e.id AS event_id,
    e.title AS event_title,
    e.status AS event_status,
    e.start_time,
    v.total_capacity,
    COUNT(DISTINCT CASE WHEN b.status = 'CONFIRMED' THEN b.id END) AS confirmed_bookings_count,
    COUNT(DISTINCT CASE WHEN b.status = 'CANCELLED' THEN b.id END) AS cancelled_bookings_count,
    COUNT(DISTINCT CASE WHEN t.status = 'VALID' THEN t.id END) AS valid_tickets_count,
    COALESCE(SUM(CASE WHEN b.status = 'CONFIRMED' THEN b.total_amount ELSE 0 END), 0.00) AS total_revenue,
    COALESCE(
        ROUND((COUNT(DISTINCT CASE WHEN t.status = 'VALID' THEN t.id END)::numeric / NULLIF(v.total_capacity, 0)::numeric) * 100, 2),
        0.00
    ) AS occupancy_percentage,
    (
        SELECT COUNT(*) 
        FROM waitlist_entries we 
        WHERE we.event_id = e.id AND we.status = 'WAITING'
    ) AS waitlist_waiting_count
FROM events e
JOIN venues v ON e.venue_id = v.id
LEFT JOIN bookings b ON b.event_id = e.id
LEFT JOIN tickets t ON t.booking_id = b.id AND b.status = 'CONFIRMED'
WHERE e.id = $1 AND e.organiser_id = $2
GROUP BY e.id, e.title, e.status, e.start_time, v.total_capacity
`

type GetEventBookingSummaryParams struct {
	ID          pgtype.UUID `json:"id"`
	OrganiserID pgtype.UUID `json:"organiser_id"`
}

type GetEventBookingSummaryRow struct {
	EventID                pgtype.UUID        `json:"event_id"`
	EventTitle             string             `json:"event_title"`
	EventStatus            EventStatus        `json:"event_status"`
	StartTime              pgtype.Timestamptz `json:"start_time"`
	TotalCapacity          int32              `json:"total_capacity"`
	ConfirmedBookingsCount int64              `json:"confirmed_bookings_count"`
	CancelledBookingsCount int64              `json:"cancelled_bookings_count"`
	ValidTicketsCount      int64              `json:"valid_tickets_count"`
	TotalRevenue           interface{}        `json:"total_revenue"`
	OccupancyPercentage    interface{}        `json:"occupancy_percentage"`
	WaitlistWaitingCount   int64              `json:"waitlist_waiting_count"`
}

func (q *Queries) GetEventBookingSummary(ctx context.Context, arg GetEventBookingSummaryParams) (GetEventBookingSummaryRow, error) {
	row := q.db.QueryRow(ctx, getEventBookingSummary, arg.ID, arg.OrganiserID)
	var i GetEventBookingSummaryRow
	err := row.Scan(
		&i.EventID,
		&i.EventTitle,
		&i.EventStatus,
		&i.StartTime,
		&i.TotalCapacity,
		&i.ConfirmedBookingsCount,
		&i.CancelledBookingsCount,
		&i.ValidTicketsCount,
		&i.TotalRevenue,
		&i.OccupancyPercentage,
		&i.WaitlistWaitingCount,
	)
	return i, err
}

const getEventCategoryBreakdown = `-- name: GetEventCategoryBreakdown :many
SELECT 
    sc.id AS seat_category_id,
    sc.name AS category_name,
    sc.color_code,
    COUNT(DISTINCT s.id) AS total_seats,
    COUNT(DISTINCT CASE WHEN t.status = 'VALID' THEN t.id END) AS booked_seats,
    COALESCE(SUM(CASE WHEN t.status = 'VALID' THEN t.unit_price ELSE 0 END), 0.00) AS revenue,
    (
        SELECT COUNT(*) 
        FROM waitlist_entries we 
        WHERE we.event_id = $1 AND we.seat_category_id = sc.id AND we.status = 'WAITING'
    ) AS waitlist_count
FROM seats s
JOIN events e ON e.venue_id = s.venue_id AND e.id = $1
JOIN seat_categories sc ON s.seat_category_id = sc.id
LEFT JOIN tickets t ON t.event_id = e.id AND t.seat_id = s.id AND t.status = 'VALID'
WHERE s.is_active = TRUE
GROUP BY sc.id, sc.name, sc.color_code
ORDER BY sc.name ASC
`

type GetEventCategoryBreakdownRow struct {
	SeatCategoryID pgtype.UUID `json:"seat_category_id"`
	CategoryName   string      `json:"category_name"`
	ColorCode      string      `json:"color_code"`
	TotalSeats     int64       `json:"total_seats"`
	BookedSeats    int64       `json:"booked_seats"`
	Revenue        interface{} `json:"revenue"`
	WaitlistCount  int64       `json:"waitlist_count"`
}

func (q *Queries) GetEventCategoryBreakdown(ctx context.Context, eventID pgtype.UUID) ([]GetEventCategoryBreakdownRow, error) {
	rows, err := q.db.Query(ctx, getEventCategoryBreakdown, eventID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []GetEventCategoryBreakdownRow{}
	for rows.Next() {
		var i GetEventCategoryBreakdownRow
		if err := rows.Scan(
			&i.SeatCategoryID,
			&i.CategoryName,
			&i.ColorCode,
			&i.TotalSeats,
			&i.BookedSeats,
			&i.Revenue,
			&i.WaitlistCount,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
